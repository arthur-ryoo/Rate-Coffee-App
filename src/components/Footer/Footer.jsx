import React from 'react';
import styles from './Footer.module.css';

const Footer = props => {
  return (
    <footer className={styles.footer}>
      <p>Copyright &copy; Rate Coffee</p>
    </footer>
  );
};

export default Footer;
